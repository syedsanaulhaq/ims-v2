# TASK DIRECTIVE: Initialize Containerized App linked to Host SQL Server (ims-docker-v1)

## 1. Context & Scope Rules
- Target Workspace Directory: `F:\ECP-Projects\ims-docker-v1`
- Environment Setup:
  - Host SQL Server Instance: `SYED-FAZLI-LAPT`
  - Target Database Name: `DockerInventoryManagementDB`
  - Host Port: `1433`
- Legacy Directory Rule: Do NOT index, scan, or process the legacy `ims-v1` directory into memory. Treat `ims-v1` as completely isolated.
- Goal: Create a clean full-stack Dockerized Express API + React setup connected directly to host SQL Server.

---

## 2. Technical Stack Specifications
- Database: Existing SQL Server on host (`SYED-FAZLI-LAPT`), database `DockerInventoryManagementDB`.
- Backend API: Node.js (Express.js) running in Docker on port `5000` using `mssql` connection pooling.
- Frontend: React (Vite + Tailwind CSS) running in Docker on port `8080`.
- Orchestration: Docker Compose v3.8 managing backend and frontend web services.

---

## 3. Project File Tree Deliverable
Please generate the complete project scaffolding matching this exact structure inside `F:\ECP-Projects\ims-docker-v1`:

ims-docker-v1/
├── docker-compose.yml
├── .gitignore
├── .env.example
├── database/
│   └── init.sql
├── backend/
│   ├── Dockerfile
│   ├── package.json
│   ├── .env
│   └── src/
│       ├── server.js
│       ├── config/
│       │   └── db.js
│       ├── routes/
│       │   ├── health.routes.js
│       │   └── inventory.routes.js
│       └── controllers/
│           ├── health.controller.js
│           └── inventory.controller.js
└── frontend/
    ├── Dockerfile
    ├── package.json
    ├── vite.config.js
    └── src/
        ├── App.jsx
        ├── main.jsx
        └── api/
            └── axiosInstance.js

---

## 4. File Contents & Code Generators

### File 1: `docker-compose.yml`
```yaml
version: '3.8'

services:
  backend:
    build: ./backend
    container_name: ims-backend
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - DB_HOST=host.docker.internal  # Connects directly to host SQL Server SYED-FAZLI-LAPT
      - DB_USER=sa
      - DB_PASSWORD=YourStrongPassword123!
      - DB_NAME=DockerInventoryManagementDB
      - DB_PORT=1433
    extra_hosts:
      - "host.docker.internal:host-gateway"

  frontend:
    build: ./frontend
    container_name: ims-frontend
    ports:
      - "8080:8080"
    depends_on:
      - backend